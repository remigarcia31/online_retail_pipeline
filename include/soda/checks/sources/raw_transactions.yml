checks for raw_transactions:
  - schema:
      fail:
        when required column missing: [TransactionID, Date, CustomerID, Gender, Age, ProductCategory, Quantity, PricePerUnit, TotalAmount]
        when wrong column type:
          TransactionID: integer
          Date: string
          CustomerID: string
          Gender: string
          Age: integer
          ProductCategory: string
          Quantity: integer
          PricePerUnit: integer
          TotalAmount: integer